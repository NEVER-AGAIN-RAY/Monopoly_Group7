import 'package:flutter/material.dart';

/// 调试页骨架，用于后续接入日志与协议测试。
class DebugPage extends StatelessWidget {
  const DebugPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Debug 页面骨架'));
  }
}
